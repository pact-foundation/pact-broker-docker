---
failure-threshold: warning

ignored:
  # Alpine and Debian package versions are not pinned: the base image moves with
  # Renovate, and a pinned package version stops resolving as soon as it does.
  - DL3008
  - DL3018
  # Every stage runs `sh`: busybox ash on Alpine, dash on Debian. Neither has
  # `pipefail`.
  - DL4006
