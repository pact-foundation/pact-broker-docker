{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices"
  ],
  "prHourlyLimit": 0,
  "prConcurrentLimit": 0,
  "rebaseWhen": "conflicted",
  "automerge": true,
  "packageRules": [
    {
      "description": "The pact_broker gem is what the image ships, so release it as a feature as soon as it is published, with no cool-off period.",
      "matchManagers": [
        "bundler"
      ],
      "matchPackageNames": [
        "pact_broker"
      ],
      "semanticCommitType": "feat",
      "minimumReleaseAge": null,
      "internalChecksFilter": "none",
      "prCreation": "immediate",
      "automerge": true,
      "automergeType": "pr",
      "schedule": [
        "at any time"
      ]
    },
    {
      "description": "compose.yml keeps a persistent volume mounted at /var/lib/postgresql/data, and Postgres 18 changed the layout of that directory, so an existing volume fails to start. Hold this file on Postgres 17 until the compose file stops reusing the old data directory.",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "postgres"
      ],
      "matchFileNames": [
        "compose.yml"
      ],
      "allowedVersions": "<18"
    }
  ]
}
