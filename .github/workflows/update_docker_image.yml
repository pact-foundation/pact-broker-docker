name: Update Docker image

on:
  push:
    branches:
      - $default-branch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - run: script/update.sh

